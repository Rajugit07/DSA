#include <iostream>
using namespace std;

int main()
{

     int a = 10;
     int *ptr = &a;

     float f = 1210.2;
     float *ptr2 = &f;

     // size of pointer
     //  cout<<sizeof(ptr)<<endl;
     //  cout<<sizeof(ptr2)<<endl;

     // pointer of pointer (to store another pointer address)

     // int **pptr = &ptr;
     // cout << &ptr << endl;
     // cout << pptr << endl;

     // cout << &a << endl;
     // cout << ptr << endl;

     // deference Operator

     // int b = 10;
     // int *bPtr = &b;
     // cout << *bPtr << endl;
     return 0;
}